#! usr/bin/bash

# check if git repo is clean - abort if not
# take version from argument - abort if not present
# run build
# run docs
# commit -m "build before release"
# npm version with version from argument
# push?